services:
  cq-server:
    build: ./server
    ports:
      - "3000:3000"
    volumes:
      - cq-data:/data
      - ./server/scripts:/app/scripts:ro
    environment:
      - CQ_DB_PATH=/data/cq.db
      - CQ_JWT_SECRET=${CQ_JWT_SECRET:?Set CQ_JWT_SECRET}

volumes:
  cq-data:
